############################################
# R ENVIRONMENT
############################################
 R_LIBS=/opt/Rlibs
 R_LIBS_USER=/opt/Rlibs
 R_LIBS_SITE=/opt/Rlibs

# Use the container’s real R installation
R_HOME=/usr/local/lib/R

# Prepend R to PATH (do not override)
PATH=/usr/local/lib/R/bin:$PATH

TAR_ASK=FALSE


############################################
# CUDA / GPU  (Matches sys_beethoven)
############################################
CUDA_VERSION=11.8.0

# CUDA_HOME explicitly set by sys_beethoven()
CUDA_HOME=/usr/local/cuda

PATH=/usr/local/cuda/bin:\
/usr/local/nvidia/bin:\
/usr/local/sbin:\
/usr/local/bin:\
/usr/sbin:\
/usr/bin:\
/sbin:\
/bin:$PATH

# Only CUDA lib64 is added — prepend, don't overwrite
LD_LIBRARY_PATH=/usr/local/cuda/lib64:$LD_LIBRARY_PATH


############################################
# OTHER TOOLS
############################################
EDITOR=vi
PAGER=/usr/bin/less


############################      CERTIFICATES      ############################
# Export CURL_CA_BUNDLE and SSL_CERT_FILE environmental variables to verify
# servers' SSL certificates during download.
CURL_CA_BUNDLE=/etc/ssl/certs/ca-certificates.crt
SSL_CERT_FILE=/etc/ssl/certs/ca-certificates.crt


############################################ THREADS ############################################
# Set default number of threads for parallel processing within R.
OMP_NUM_THREADS=1
LIGHTGBM_NUM_THREADS=1
